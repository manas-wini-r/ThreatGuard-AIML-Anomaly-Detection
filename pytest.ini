[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --cov=app --cov-report=html --cov-report=term
asyncio_mode = auto
# Remove or lower the fail-under
# --cov-fail-under=70  # Comment this out or change to 20